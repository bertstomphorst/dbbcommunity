<odoo>
    <record id="view_partner_simple_form" model="ir.ui.view">
        <field name="name">res.partner.form.simple.salutation</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_simple_form" />
        <field name="arch" type="xml">
            <xpath expr="//field[@name='function']" position="before">
                <field name="salutation" invisible="type != 'contact'"/>
            </xpath>
        </field>
    </record>

    <record id="view_partner_form" model="ir.ui.view">
        <field name="name">res.partner.form.salutation</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form" />
        <field name="arch" type="xml">
            <xpath expr="//span[@name='address_name']" position="before">
                <field name="is_company" invisible="1"/>
                <field name="salutation" invisible="is_company"/>
            </xpath>

            <xpath expr="//field[@name='child_ids']/form//field[@name='title']" position="after">
                <field name="is_company" invisible="1"/>
                <field name="salutation" invisible="is_company"/>
            </xpath>
        </field>
    </record>
</odoo>
